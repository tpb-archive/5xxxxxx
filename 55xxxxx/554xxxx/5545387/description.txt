I've taken another version and re-encoded/re-compressed it using standard codecs.
The files are now much smaller than the original and using standard MP3 as audio (original was AAC that couldn't be played on standalone players).