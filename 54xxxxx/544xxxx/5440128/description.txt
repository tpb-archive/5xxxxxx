Anti-P2P organizations are actively polluting P2P networks with fake peers, which send out fake or corrupt data in order to waste bandwidth and slow down file transfers. 

Once youâ€™ve established a connection to one of these fake â€œpeersâ€, your IP has been logged. 

But if you are using ÂµTorrent (and other torrent programs that using dat files) you can stop it by employing a little known feature called IP filtering.

Read the help file in Utorrent how to apply it.

Normaly it should be stored in: ApplicationDatauTorrent

This one is compressed so use winrar or other program to extract the file.