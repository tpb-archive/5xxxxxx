This is the amtlib.dll file + the disable of adobe activation servers

First extract the files into a folder and run hosts.bat
then copy amtlib.dll into the program folder


NOTE: You should already have a keygen. This file is only used to prevent from showing errors like:
This serial is already in used
This serial is expired

etc, etc

------------------------------------>TONiG