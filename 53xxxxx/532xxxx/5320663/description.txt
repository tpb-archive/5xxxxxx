just mount with deamon tools lite or another program like that and click on setup simple as that once installed goto the crack folder and copy the crack to the red alert file location then run as admin